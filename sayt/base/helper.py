# import base64


# def code_decoder(code, decode=False):
#
#     if decode:
#         return base64.b64decode(code).decode()
#
#     else:
#         return base64.b64encode(str(code).encode()).decode()
